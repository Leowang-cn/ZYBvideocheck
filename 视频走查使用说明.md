# 视频走查使用说明

本文面向日常视频走查人员。当前推荐流程通过 OpenList 读取百度个人网盘视频，只上传截图到腾讯 COS，不下载完整原片，也不进行视频转码。

## 一、使用前确认

每次开始前确认：

1. OpenList 已启动。
2. OpenList 中的百度个人网盘可以正常打开。
3. 待走查视频已经上传到配置的百度网盘目录。
4. 内网视频走查页面可以正常访问。
5. 本机网络能够访问 OpenList、腾讯 COS 和内网服务器。

本机 `.env` 已配置后，日常使用时不需要修改。不要把 `.env`、Token、密码或密钥发送给其他人。

## 二、百度网盘目录规则

当前扫描目录由 `.env` 中的 `OPENLIST_PATH` 决定，例如：

```text
OPENLIST_PATH=/baidu-test/初化
BAIDU_PAN_MOUNT_PATH=/baidu-test
```

报告中的地址按百度网盘挂载点后的目录计算。例如：

```text
/baidu-test/初化/课程A/video.mp4
```

对应：

- 一级地址：`初化`
- 二级地址：`课程A`

如果视频位于：

```text
/baidu-test/初化/video.mp4
```

则一级地址为 `初化`，二级地址为空。目录超过两级时，只取挂载点后的前两级作为一级、二级地址。

## 三、每周操作步骤

1. 将本周视频上传到百度个人网盘的目标目录。
2. 打开 OpenList，确认新视频可以正常看到和访问。
3. 双击项目根目录中的 `运行视频走查.command`。
4. 输入本次批次名称，例如 `2026年第34周`；直接回车则使用当天日期。
5. 等待程序完成。程序会自动查找视频、读取视频信息、生成截图、上传截图、导入内网服务器并更新本地 HTML 报告。
6. 终端显示“处理完成”后，打开内网视频走查页面查看结果。

处理远程视频时，程序通过 OpenList 临时地址按需读取片段，不会完整下载视频。

## 四、截图规则

每个视频默认生成：

- 第 3 秒截图；
- 倒数第 3 秒截图；
- 视频严格大于 6 秒时，再生成中间位置截图。

截图保持原视频分辨率，包括 4K 视频。

远程模式不会完整下载百度网盘视频、转码原视频或将原视频上传到 COS，只会上传截图。

## 五、内网页面使用

内网视频走查页面支持：

- 按一级地址、二级地址、批次和创建日期筛选；
- 按视频名称搜索；
- 查看原分辨率截图；
- 打开百度网盘中的原视频目录；
- 填写评级、质检备注和补充信息；
- 导出当前筛选结果为 CSV。

“打开原视频”会进入百度个人网盘官方目录页面。访问者需要登录具有该文件权限的百度账号，并根据页面中的精确文件名定位原片。

## 六、重复运行

系统会保存处理历史，同一个视频重复运行时不会重复上传或重复导入。

终端显示：

```text
没有发现需要处理的新视频。
```

通常表示当前目录中的视频都已经处理完成，不是故障。

远程视频根据 OpenList 路径、文件大小和修改时间判断是否为同一个文件。如果这些信息发生变化，系统可能将其作为新视频重新处理。

## 七、本地备用输入

除 OpenList 远程目录外，也可以将本地视频放入：

```text
待处理视频/
```

该目录支持子文件夹，运行方式相同。本地模式会上传本地原视频和截图；OpenList 远程模式只上传截图。

支持的视频格式：

```text
.mp4 .mov .mkv .avi .m4v .webm
```

## 八、本地输出和历史

本地累计报告：

```text
输出/视频走查.html
```

运行历史数据库：

```text
数据/import-history.sqlite
```

不要手动删除历史数据库，否则已经处理过的视频可能被重新上传和导入。

## 九、常见问题

### OpenList 连接失败

确认 OpenList 已启动，并检查 `.env` 中的 `OPENLIST_URL`。默认本机地址为：

```text
http://127.0.0.1:5244
```

### OpenList Token 无效

如果提示 `token is invalidated`，重新登录 OpenList，在浏览器控制台读取：

```javascript
localStorage.getItem("token")
```

将新值更新到 `.env` 的 `OPENLIST_TOKEN`。OpenList Token 不是百度网盘 Refresh Token，也不是服务器导入 Token。

### 找不到视频

检查：

- `OPENLIST_PATH` 是否正确；
- 视频是否位于扫描目录或其子目录；
- 视频扩展名是否受支持；
- OpenList 中是否能正常看到该文件。

### 内网服务器导入失败

先确认内网页面能够打开。服务器暂时不可用时，已经成功上传的截图和本地历史会保留；服务器恢复后重新运行即可继续导入。

### 部分视频处理失败

终端会显示具体失败文件和原因，其他成功文件仍会保留结果。修复网络、Token 或文件问题后重新运行即可。

## 十、安全注意事项

- 不要提交或分享 `.env`。
- 不要在截图中显示 Token、密码或 COS 密钥。
- `OPENLIST_TOKEN`、`VIDEO_REVIEW_IMPORT_TOKEN` 和 COS 密钥用途不同，不要混用。
- Token 意外暴露后应立即轮换。
- 不要删除服务器 PostgreSQL 数据卷。
- 不要删除本地 `数据/import-history.sqlite`，除非明确需要重建全部历史。
