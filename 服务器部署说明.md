# 视频走查服务器部署说明

本文档供技术同学部署使用。首期通过公司内网 IP 和端口访问，不需要公网域名；腾讯 COS 由业务侧维护，服务器不存储视频和截图。

## 1. 交付内容

- `server/`：FastAPI 服务端、数据库模型和 Web 页面。
- `Dockerfile`：应用镜像。
- `compose.yaml`：应用、PostgreSQL、Nginx 编排。
- `deploy/nginx.conf`：内网反向代理配置。
- `deploy/server.env.example`：Docker Compose 配置模板。
- `deploy/video-review.service`：不使用 Docker 时的 systemd 模板。
- `deploy/verify-deployment.sh`：部署后接口验收脚本。
- `deploy/backup-postgres.sh`：PostgreSQL 备份脚本模板。
- `deploy/build-server-package.sh`：技术交付包生成脚本。
- `requirements-server.txt`：生产服务端 Python 依赖。
- `requirements-server-test.txt`：服务端测试依赖。
- `tests/test_server.py`：服务端自动化测试。

## 2. 推荐部署：Docker Compose

环境要求：Linux、Docker Engine、Docker Compose 插件，建议配置为 2 核 CPU、4 GB 内存、50 GB SSD。

将项目上传到服务器，例如 `/opt/video-review`，进入项目目录后执行：

```sh
cp deploy/server.env.example .env.server
chmod 600 .env.server
```

编辑 `.env.server`，必须替换：

- `POSTGRES_PASSWORD`：PostgreSQL 应用账号密码。
- `IMPORT_TOKEN`：本地导入脚本调用 API 的独立 Token，建议使用至少 32 字节随机值。
- `LISTEN_PORT`：内网访问端口，默认 `8080`。

密码可以包含 `@`、`:` 等特殊字符。Compose 会通过独立环境变量传入密码，不需要手工进行 URL 编码。

启动并检查：

```sh
docker compose --env-file .env.server up -d --build
docker compose --env-file .env.server ps
curl http://127.0.0.1:8080/api/health
```

健康检查应返回：

```json
{"status":"ok"}
```

随后通过 `http://服务器内网IP:8080` 访问页面。技术侧需要限制该端口仅允许目标公司内网网段访问，不应把 PostgreSQL 端口映射到宿主机。

可使用以下脚本验证健康检查、导入鉴权、幂等导入和列表查询：

```sh
sh deploy/verify-deployment.sh http://服务器内网IP:8080 "实际IMPORT_TOKEN"
```

## 3. 使用公司现有 PostgreSQL

如使用公司数据库，不需要启动 `compose.yaml` 中的 `database` 服务。应用连接串格式为：

```text
postgresql+psycopg://用户名:密码@数据库地址:5432/数据库名
```

将 `DATABASE_URL` 和 `IMPORT_TOKEN` 作为应用进程环境变量。数据库账号需要对指定数据库拥有建表、建索引和读写权限。应用首次启动会创建 `videos`、`snapshots` 和 `reviews` 表。

## 4. 不使用 Docker：systemd

服务器建议使用 Python 3.12。以下命令中的系统账号创建、目录授权和 Nginx 安装由技术按公司规范执行：

```sh
cd /opt/video-review
python3.12 -m venv .venv
./.venv/bin/pip install -r requirements-server.txt
```

创建 `/etc/video-review/server.env`：

```text
DATABASE_URL=postgresql+psycopg://用户名:密码@数据库地址:5432/数据库名
IMPORT_TOKEN=高强度随机Token
```

将 `deploy/video-review.service` 安装到 `/etc/systemd/system/`，根据实际部署账号和路径调整后启动：

```sh
systemctl daemon-reload
systemctl enable --now video-review
systemctl status video-review
```

Nginx 将内网端口反向代理到 `127.0.0.1:8000`。如沿用 `deploy/nginx.conf`，需要把上游 `app:8000` 改为 `127.0.0.1:8000`。

## 5. 本地脚本接入

部署完成后，技术向业务侧提供：

- 页面和 API 地址，例如 `http://10.x.x.x:8080`。
- 与服务器 `IMPORT_TOKEN` 完全相同的导入 Token。

在本地项目 `.env` 中新增：

```text
VIDEO_REVIEW_SERVER_URL=http://10.x.x.x:8080
VIDEO_REVIEW_IMPORT_TOKEN=与服务器一致的Token
```

之后继续双击 `运行视频走查.command`。脚本在视频和截图上传 COS 后，会把结构化记录推送到服务器。服务端按视频 SHA-256 幂等导入，重复运行不会重复新增。

若暂时清空 `VIDEO_REVIEW_SERVER_URL`，本地工具仍会按原方式生成累计 HTML，不调用服务器。

## 6. 验收清单

1. `GET /api/health` 返回 HTTP 200 和 `{"status":"ok"}`。
2. 内网电脑可以打开服务器 IP 和端口，非授权网络无法访问。
3. 使用错误 Token 调用 `POST /api/videos/import` 返回 HTTP 401。
4. 本地脚本处理一条测试视频后，页面出现对应视频、截图和元数据。
5. 重复推送同一条视频后，记录总数不增加。
6. 页面可以筛选、分页、查看截图、保存评级和备注、导出 CSV。
7. 服务重启后记录和质检结果仍然存在。
8. PostgreSQL 未对办公网络或公网开放。

可在部署前运行自动化测试：

```sh
./.venv/bin/pip install -r requirements-server-test.txt
./.venv/bin/python -m unittest tests.test_server -v
```

## 7. 运维与备份

- 每天备份 PostgreSQL，建议保留 30 天，并定期进行恢复演练。
- `deploy/backup-postgres.sh` 可作为 Docker Compose 部署的备份脚本模板，技术应按实际目录调整并加入定时任务。
- 恢复前应停止应用写入，在临时数据库验证备份完整性后，再由数据库管理员执行恢复。
- Docker 部署的数据在 `postgres_data` 卷中；升级应用时不得删除该卷。
- 更新代码后执行 `docker compose --env-file .env.server up -d --build`。
- 查看应用日志：`docker compose --env-file .env.server logs --tail=200 app`。
- 查看 Nginx 日志：`docker compose --env-file .env.server logs --tail=200 nginx`。
- systemd 部署查看日志：`journalctl -u video-review -n 200 --no-pager`。
- 建议由监控系统定期请求 `/api/health`，失败时告警。

Docker Compose 备份脚本默认用法：

```sh
PROJECT_DIR=/opt/video-review BACKUP_DIR=/var/backups/video-review \
	sh deploy/backup-postgres.sh
```

恢复命令示例，执行前必须确认目标数据库并做好现状备份：

```sh
gzip -dc /var/backups/video-review/video-review-时间.sql.gz | \
	docker compose --env-file .env.server exec -T database \
	psql --username video_review --dbname video_review
```

## 8. 当前安全边界

导入接口必须使用独立 Token。页面、列表、质检保存和 CSV 接口首期没有应用内登录，依赖公司内网、防火墙和 Nginx 访问范围控制。因此部署时必须确保服务只在受控内网开放。

如后续需要区分修改人身份、角色权限或接入公司统一认证，应在开放给更大范围前增加登录鉴权和审计日志。本版本已使用质检数据版本号避免多人同时保存时静默覆盖。

## 9. 数据迁移说明

本版本为首期建库，启动时自动创建表。后续如修改数据库字段，应引入正式数据库迁移工具并在升级前备份；不要通过删除数据卷或删除数据库的方式升级。