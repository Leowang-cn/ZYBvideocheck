# 视频走查系统技术交付清单

## 交付范围

请将项目目录整体提供给技术，但不要包含以下本地文件和目录：

- `.env`、`.env.server`：包含密钥或密码。
- `.venv/`：本机 Python 环境。
- `待处理视频/`、`输出/`：本地媒体和报告。
- `数据/`：本地 SQLite 历史数据。

Docker 构建时，[.dockerignore](.dockerignore) 会自动排除上述内容。通过压缩包或文件传输工具交付时，也需要遵守同样边界。

推荐在项目根目录执行：

```sh
sh deploy/build-server-package.sh
```

该命令生成 `视频走查服务器交付包.tar.gz`，其中只包含服务器部署所需文件、服务端测试和技术文档。

## 技术部署必需文件

- `server/`：服务端代码和内网页面。
- `requirements-server.txt`：生产服务端依赖及固定版本。
- `requirements-server-test.txt`：服务端验收测试依赖。
- `Dockerfile`：应用镜像定义和健康检查。
- `compose.yaml`：FastAPI、PostgreSQL、Nginx 服务编排。
- `deploy/nginx.conf`：Nginx 反向代理配置。
- `deploy/server.env.example`：生产环境变量模板。
- `deploy/video-review.service`：systemd 备选配置。
- `deploy/verify-deployment.sh`：部署后接口验收脚本。
- `deploy/backup-postgres.sh`：PostgreSQL 备份脚本模板。
- `deploy/build-server-package.sh`：生成不含本地媒体和密钥的交付压缩包。
- `服务器部署说明.md`：部署、验收、更新、备份和安全说明。

## 业务侧保留文件

- `video_review/`：本地视频处理、COS 上传和服务器推送代码。
- `.env.example`：本地 COS 与服务器连接配置模板。
- `运行视频走查.command`：业务侧日常运行入口。
- `README.md`：本地工具使用说明。

技术部署服务器时不需要业务侧的 COS 密钥。服务端只接收视频元数据与 COS URL，不上传、下载或保存媒体。

## 部署前双方确认值

技术提供：

- 服务器内网 IP。
- 页面访问端口，默认建议 `8080`。
- `IMPORT_TOKEN`，由技术生成并安全发给业务侧。
- 部署完成时间和维护联系人。

业务侧提供：

- 需要允许访问页面的公司内网范围。
- 一条用于上线验收的视频。
- 预计开始使用日期。

不需要提供固定域名、公网入口或 COS 配置。

## 推荐交付顺序

1. 技术先阅读 [服务器部署说明.md](服务器部署说明.md)。
2. 技术按 [deploy/server.env.example](deploy/server.env.example) 创建服务器私有配置。
3. 技术部署并运行 `deploy/verify-deployment.sh`。
4. 业务侧在本地 `.env` 填入服务器地址和 Token。
5. 业务侧上传一条真实测试视频，双方完成页面验收。
6. 技术配置数据库定时备份和健康检查告警。

## 当前版本边界

- 页面和质检接口首期没有应用内登录，必须限制为受控公司内网访问。
- 导入接口使用 Bearer Token 鉴权。
- 数据库表在首次启动时自动创建。
- 后续数据库结构变更应引入迁移工具，不能通过删除数据库或 Docker 卷升级。
- 服务器不保存视频和截图，页面能否加载媒体取决于客户端网络能否访问对应 COS URL。