# 视频走查工具第一版方案共识

更新时间：2026-08-17

## 1. 目标

实现一个在本地手动运行的视频处理工具，将已从百度企业网盘下载到本地的视频批量处理并上传至腾讯 COS，同时生成包含内嵌截图的离线 HTML 表格，供质检员和领导查阅。

当前业务规模：

- 每周约 100 个视频。
- 单个视频不超过 1 GB。
- 视频平均时长约 20 分钟。
- 1 名质检员。
- 最多 3 名领导查阅。
- 评级固定为四档：优秀、良好、合格、不合格。
- 评级原因暂不结构化，使用开放性备注。

## 2. 已确认流程

1. 质检员手动从百度企业网盘下载本批视频。
2. 将视频放入约定的本地待处理文件夹。
3. 质检员手动运行脚本，不设置定时任务。
4. 脚本扫描待处理文件夹并识别本批尚未成功处理的视频。
5. 脚本读取视频时长、文件大小和分辨率等信息。
6. 脚本使用 FFmpeg 生成固定截图。
7. 脚本将视频和截图上传至腾讯 COS。
8. 脚本将本批成功记录加入固定的离线 HTML 总表。
9. 质检员在浏览器中打开 HTML，或将文件发送给相关人员查阅。
10. 质检员在钉钉中填写评级和质检备注。
11. 领导在钉钉中查阅结果并填写补充信息。

## 3. 第一版范围

脚本负责：

- 扫描指定的本地视频目录。
- 支持常见视频格式，具体格式在开发时根据真实样本确定。
- 通过 ffprobe 获取时长、分辨率和编码等元数据。
- 通过 FFmpeg 生成 JPEG 截图。
- 将原视频和截图上传腾讯 COS。
- 验证上传结果。
- 生成固定的 `.html` 离线总表，截图直接内嵌在单个文件中。
- 在本地记录处理历史，避免重复上传和重复进入 HTML。
- 单个视频失败时继续处理其他视频。
- 输出成功、跳过和失败数量以及失败原因。
- 再次运行时重试未成功的视频。

第一版不包含：

- 百度网盘开放 API 接入。
- 百度网盘自动下载或客户端自动化操作。
- 钉钉开放 API 接入。
- 浏览器自动操作钉钉文档。
- 在线质检网站或业务服务器。
- 服务端数据库。
- 自动定时执行。
- 视频转码。
- 自动判断视频是否合格。

## 4. 截图规则

- 每个视频截取第 3 秒和倒数第 3 秒。
- 视频时长严格大于 6 秒时，再截取视频中间位置。
- 每个视频一共生成 2 至 3 张截图；极短视频的时间点自动限制在有效范围内。
- 截图一旦生成，同一视频重复处理时保持不变。
- 截图格式为高质量 JPEG，保持源视频原始分辨率和比例，包括 4K，不进行缩放。
- HTML 表格中使用固定尺寸缩略展示，图片查看器加载同一张原分辨率截图。
 HTML 中记录所有实际截图时间点。
- 第一版不做黑屏检测；真实使用中若频繁截到黑场，再增加候选时间点重试。

## 5. HTML 内容

首次成功处理后生成 `输出/视频走查.html`；后续运行只上传新增视频，并根据本地 SQLite 历史重建同一个累计总表。总表保留历史记录及所属批次，提供一级地址、二级地址、视频名称、批次、创建日期和评级筛选，并可将当前筛选结果导出为 CSV。

建议字段：

| 字段 | 内容 | 填写方 |
| --- | --- | --- |
| 一级地址 | 相对 `待处理视频` 的第一个文件夹，不存在则为空 | 脚本 |
| 二级地址 | 相对 `待处理视频` 的第二个文件夹，不存在则为空 | 脚本 |
| 创建时间 | 首次创建本条数据的日期，精确到日 | 脚本 |
| 批次 | 本次处理批次名称 | 脚本 |
| 视频名称 | 原始文件名 | 脚本 |
| 视频链接 | COS 长期访问 URL | 脚本 |
| 截图 | HTML 内嵌缩略图 | 脚本 |
| 截图链接 | COS 长期访问 URL | 脚本 |
| 文件大小 | 格式化后的文件大小 | 脚本 |
| 视频时长 | `HH:MM:SS` | 脚本 |
| 分辨率 | 如 `1920 x 1080` | 脚本 |
| 截图时间点 | `HH:MM:SS` | 脚本 |
| 评级 | 优秀、良好、合格、不合格 | 质检员 |
| 质检备注 | 开放性文本 | 质检员 |
| 补充 | 开放性文本 | 领导 |
| 导入时间 | 脚本处理时间 | 脚本 |

HTML 中为“评级”提供下拉选项，同时保留质检备注和补充输入框。截图以内嵌数据形式保存在同一个 HTML 文件中，并同时提供 COS 原图链接。点击缩略图时在原页面打开图片查看器，可切换上一张和下一张，不跳转新页面。CSV 只导出当前筛选条件下的可见行，视频和截图字段仅提供 COS URL，不写入媒体文件或 Base64 图片。

## 6. 钉钉使用约定

- 长期维护一张视频走查总表。
- 需要共享或归档时发送当前的 `视频走查.html` 总表。
- 不使用整表覆盖方式导入，避免丢失历史评级和备注。
- 质检员填写评级和质检备注。
- 领导主要查阅，可以填写独立的“补充”字段，不覆盖质检结果。
- 建议在钉钉中建立待质检、本周已完成、不合格和领导查阅等视图。

## 7. 去重与失败恢复

脚本在本地保存处理历史，建议使用 SQLite。该记录仅服务于导入流程，不保存钉钉中的评级结果。

至少记录：

- 本地文件路径。
- 文件大小和修改时间。
- 文件指纹或唯一 ID。
- COS 视频 Object Key。
- COS 截图 Object Key。
- 视频和截图上传状态。
- 截图时间点。
- HTML 导出状态和批次。
- 错误信息和最后处理时间。

行为约定：

- 已成功处理并导出的文件再次出现时跳过。
- 同名但内容不同的文件视为不同视频。
- 上次失败的文件允许再次运行时重试。
- 视频已成功上传但后续步骤失败时，不重复上传视频。
- 只有视频和截图均成功上传的记录才进入正式 HTML。
- 某个视频失败不终止整个批次。

## 8. 腾讯 COS 前提

离线 HTML 中的链接需要支持领导长期查阅，因此第一版需要长期有效的视频和截图访问 URL。

约定如下：

- 使用专门规划的 COS 公有读桶或公有读存储区域。
- 不将包含其他资料的现有桶整体改为公有读。
- 上传权限只授予本地脚本使用的最小权限子账号。
- 数据对象使用不可猜测的唯一 ID 组织路径，避免重名和简单枚举。
- COS SecretId 和 SecretKey 只保存在本地环境配置中，不写入代码或 HTML。
- 如果最终只能使用私有桶，签名 URL 会过期，当前纯离线方案需要重新评估，不能直接按本共识实施。

对象路径建议：

```text
video-review/{year-week}/{video-id}/video.{ext}
video-review/{year-week}/{video-id}/snapshot.jpg
```

## 9. 本地操作形式

第一版面向 macOS 使用，可提供一个可双击执行的入口，例如：

```text
运行视频走查.command
```

每次运行时允许输入或确认批次名称。处理完成后输出摘要和 HTML 路径，例如：

```text
扫描文件：100
历史已处理：5
本次待处理：95
成功：93
失败：2
HTML：输出/视频走查.html
```

如果本次没有新增视频，不生成空 HTML。

## 10. 开发前待准备

1. 本地待处理视频文件夹路径。
2. 需要支持的真实视频格式样本。
3. 腾讯 COS Bucket 名称。
4. 腾讯 COS Region。
5. 最小权限子账号的 SecretId 和 SecretKey，仅在本地配置。
6. 视频和截图的长期公共访问域名或 URL 规则。
7. 本地是否已安装 FFmpeg 和 ffprobe；未安装时由开发阶段补充安装说明。

## 11. 第一批验收标准

使用 5 个真实视频验证：

1. 脚本能识别待处理目录中的全部视频。
2. 时长、大小和分辨率读取正确。
3. 截图时间和截图内容符合约定。
4. 视频和截图均成功上传 COS。
5. HTML 中名称、链接、内嵌截图和元数据正确。
6. 点击 HTML 中的视频及截图链接可以长期访问。
7. HTML 文件在常用浏览器中中文、图片、链接和字段位置正确。
8. 钉钉中可以填写四档评级、质检备注和补充。
9. 重复运行不会重复上传或重复导出同一视频。
10. 单个视频失败时，其余视频仍能完成处理。

## 12. 最终共识

第一版建设一个本地手动运行的视频走查工具：质检员先从百度企业网盘手动下载视频，脚本负责读取本地视频、固定截帧、上传视频和截图至腾讯 COS，并生成单文件离线 HTML；质检员和领导使用浏览器打开或通过钉钉发送该文件进行查阅。静态 HTML 中的评级和备注输入不会持久保存，正式标记仍需另行记录。
