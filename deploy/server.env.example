# 复制为项目根目录的 .env.server，并设置为仅部署账号可读。
POSTGRES_PASSWORD=请替换为高强度随机密码
IMPORT_TOKEN=请替换为高强度随机Token
LISTEN_PORT=8080

# 仅用于不用 compose、直接运行应用的部署方式：
# DATABASE_URL=postgresql+psycopg://video_review:密码@数据库地址:5432/video_review